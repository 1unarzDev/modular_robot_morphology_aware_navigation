# Plan over a finite catalog of validated morphologies

The planner searches a finite morphology transition graph instead of synthesizing arbitrary module graphs online. This keeps collision checking, controller selection, stability, and physical redocking certifiable while preserving the central pose–morphology planning question; general reconfiguration synthesis can later supply additional validated catalog entries.

